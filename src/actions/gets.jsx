//import * as api from '../api';
//import { fetchGets } from '../api/index-api';
//import { fetchGets } from '../api/index-api';

/*import axios from "axios";

const url = 'http://localhost:3000/login';

const fetchGets = ()=> axios.get(url);

export const getGets = () => async (dispatch)=> {
    
    try {
        const { data } = await fetchGets();
        dispatch({type: 'FETCH_ALL', payload: data});
    }
    catch (error){
        console.log(error)
    }



    const action = {type: 'FETCH_ALL', payload: []}; //this is an object

    return action;
}*/